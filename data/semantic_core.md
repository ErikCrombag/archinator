# ArchiMate 3.2 Semantic Core Reference

> This file is a placeholder. Run `scripts/bootstrap.py` to generate the full
> semantic core from the ArchiMate 3.2 PDF and the OpenGroup website.
>
> Until then, Archinator will fall back to built-in rules in `validation/rules.py`.

## Status
Pending bootstrap — provide `data/archimate_spec.pdf` and run:
```
python scripts/bootstrap.py --pdf data/archimate_spec.pdf
```
